---
import HomePage from "../components/Home/HomePage";
import { staticT } from "../i18n";
import Layout from "../layouts/Layout.astro";
const title = staticT("seo.pages.home");
const pathName = "/";
---

<Layout title={title} pathName={pathName}>
	<HomePage client:load />
</Layout>
